pipeline{
    agent {label 'master'}
    stages{
        stage('Unit-Testing'){
            steps{
                sh 'mvn test'
            }
        }
        stage('Publishing-Junit-Test'){
            steps{
                  step([$class: 'JUnitResultArchiver', testResults: 'gameoflife-core/target/surefire-reports/*.xml'])
            }
        }
        stage('build && SonarQube analysis'){
            steps{
                ithSonarQubeEnv('Sonar-1'){
                    sh 'mvn clean package sonar:sonar'
                }
            }
        }
    }
}